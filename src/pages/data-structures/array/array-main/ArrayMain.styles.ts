import styled from 'styled-components';

const ArrayMain = styled.section`
  display: flex;
  flex-direction: column;
  gap: 2rem;
  align-items: center;
  font-size: var(--fs-m);
`;

export { ArrayMain };
