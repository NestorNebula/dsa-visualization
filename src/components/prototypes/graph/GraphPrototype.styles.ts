import styled from 'styled-components';

const GraphPrototype = styled.div``;
const Container = styled.div``;
const Edges = styled.div``;
const Edge = styled.button``;

export { GraphPrototype, Container, Edges, Edge };
